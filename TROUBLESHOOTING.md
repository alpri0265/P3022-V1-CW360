# 🔧 Діагностика проблем з LCD

## Проблема: Екран світиться з квадратиками, але меню не відображається

### ✅ Що вже перевірено:
- ✅ Екран світиться (підсвітка працює)
- ✅ Квадратики реагують на потенціометр контрасту (підключення V0 працює)

---

## 📋 Чек-лист діагностики (перевіряйте по порядку):

### 1. **КОНФІГУРАЦІЯ КОДУ** ✅ (ВИПРАВЛЕНО)

**Перевірте файл `Config.h`:**

```cpp
// Має бути так:
#define LCD_TYPE_1602
#define LCD_INTERFACE_PARALLEL_4BIT  // ← ЦЕ МАЄ БУТИ АКТИВНО!
// #define LCD_INTERFACE_I2C          // ← ЦЕ МАЄ БУТИ ЗАКОМЕНТОВАНО!
```

**Якщо не так:**
- Відкрийте `Config.h`
- Закоментуйте `#define LCD_INTERFACE_I2C`
- Розкоментуйте `#define LCD_INTERFACE_PARALLEL_4BIT`
- Збережіть та перекомпілюйте скетч
- Завантажте на Arduino

---

### 2. **ПЕРЕВІРКА ПІДКЛЮЧЕННЯ ПІНІВ**

**Для Arduino Micro з LCD 1602 (4-bit):**

| LCD Pin | Arduino Micro Pin | Перевірка |
|---------|-------------------|-----------|
| VSS (GND) | GND | ✅ Перевірте мультиметром |
| VDD (VCC) | 5V | ✅ Перевірте мультиметром |
| V0 (Contrast) | Потенціометр → GND | ✅ Вже працює (квадратики реагують) |
| RS | D12 (PIN 12) | ⚠️ **ПЕРЕВІРТЕ** |
| E (Enable) | D11 (PIN 11) | ⚠️ **ПЕРЕВІРТЕ** |
| D4 | D7 (PIN 7) | ⚠️ **ПЕРЕВІРТЕ** |
| D5 | D6 (PIN 6) | ⚠️ **ПЕРЕВІРТЕ** |
| D6 | D5 (PIN 5) | ⚠️ **ПЕРЕВІРТЕ** |
| D7 | D8 (PIN 8) | ⚠️ **ПЕРЕВІРТЕ** |
| RW | GND | ✅ Має бути підключено до GND |
| A (Anode) | 5V через резистор 220Ω | ✅ Підсвітка працює |
| K (Cathode) | GND | ✅ Підсвітка працює |

**Важливо:**
- Піни D4-D7 на LCD **НЕ** плутати з D4-D7 на Arduino!
- LCD D4 → Arduino D7
- LCD D5 → Arduino D6
- LCD D6 → Arduino D5
- LCD D7 → Arduino D8

---

### 3. **КОНФЛІКТ ПІНІВ З ЕНКОДЕРОМ**

**Перевірте конфлікти:**

| Компонент | Пін | Статус |
|-----------|-----|--------|
| Енкодер A | D2 | ✅ OK |
| Енкодер B | D3 | ✅ OK |
| Енкодер SW | D4 | ✅ OK |
| LCD RS | D12 | ✅ OK (немає конфлікту) |
| LCD E | D11 | ✅ OK (немає конфлікту) |
| LCD D4-D7 | D5, D6, D7, D8 | ✅ OK (немає конфлікту) |

**Увага:** На Arduino Micro піни D2 і D3 використовуються для USB Serial, але це не заважає роботі енкодера.

---

### 4. **ПРОБЛЕМИ З ЖИВЛЕННЯМ**

**Симптоми:**
- Екран світиться, але показує тільки квадратики
- Може бути недостатньо струму

**Перевірка:**
- ✅ Підключіть LCD VCC до стабільного 5V
- ✅ Перевірте GND (має бути спільний GND між Arduino і LCD)
- ⚠️ Якщо використовуєте USB для живлення - можливо недостатньо струму
- 💡 Спробуйте живлення через адаптер 5V/1A

---

### 5. **ПРОБЛЕМИ З КОНТРАСТОМ (V0)**

**Симптоми:**
- Квадратики видно, але текст не видно
- Текст дуже блідий або зовсім не видно

**Рішення:**
1. Повільно крутіть потенціометр контрасту (V0)
2. Спостерігайте за екраном
3. Знайдіть оптимальне положення, де текст найкраще видно
4. Якщо не допомагає - перевірте підключення V0:
   - V0 → Середній вивід потенціометра
   - Один край потенціометра → VCC (5V)
   - Інший край потенціометра → GND

---

### 6. **ПРОБЛЕМИ З ІНІЦІАЛІЗАЦІЄЮ**

**Якщо код не ініціалізує LCD:**

**Перевірте в Arduino IDE:**
1. Відкрийте Serial Monitor (Tools → Serial Monitor)
2. Встановіть швидкість 9600 baud
3. Додайте в код діагностичні повідомлення (див. нижче)

**Тимчасовий діагностичний код:**

Додайте в `setup()` після `lcdDisplay.showStartup()`:

```cpp
Serial.begin(9600);
delay(1000); // Wait for Serial
Serial.println("LCD initialization test...");
Serial.println("If you see this, Arduino is working");
```

---

### 7. **ПРОБЛЕМИ З БІБЛІОТЕКОЙ LiquidCrystal**

**Перевірте:**
- ✅ Бібліотека `LiquidCrystal` встановлена (входить в стандартний набір Arduino IDE)
- ✅ Версія Arduino IDE: 1.8.x або новіша
- ✅ Вибрано правильну плату: **Tools → Board → Arduino Micro**

**Якщо помилки компіляції:**
```
error: 'LiquidCrystal' does not name a type
```
→ Перевірте, що `#define LCD_INTERFACE_PARALLEL_4BIT` активний в `Config.h`

---

### 8. **ПРОБЛЕМИ З ЕНКОДЕРОМ (може блокувати роботу)**

**Перевірте:**
- Енкодер A → D2
- Енкодер B → D3
- Енкодер SW → D4
- Загальний GND між енкодером і Arduino

**Тест енкодера:**
- Покрутіть енкодер - чи працює кнопка?
- Натисніть кнопку енкодера - чи є реакція?

---

### 9. **ДЕТАЛЬНА ПЕРЕВІРКА ПІНІВ (Мультиметром)**

**Якщо маєте мультиметр:**

1. **Перевірка напруги:**
   - VCC (LCD пін 2) → має бути 5V ±0.1V
   - GND (LCD пін 1) → має бути 0V

2. **Перевірка сигналів під час роботи:**
   - RS (D12) → має змінюватись під час роботи
   - E (D11) → має "мблигати" (часті імпульси)
   - D4-D7 (D7, D6, D5, D8) → мають змінюватись

3. **Перевірка опору:**
   - RW → GND: має бути ~0 Ом
   - Перевірте всі з'єднання на коротке замикання

---

### 10. **ТЕСТОВИЙ КОД ДЛЯ ДІАГНОСТИКИ**

⚠️ **ВАЖЛИВО:** Тестовий скетч має бути в **окремій папці**, не в папці основного проекту!

**Як створити тестовий проект:**

1. **В Arduino IDE:**
   - File → New (або Ctrl+N)
   - File → Save As → збережіть як `LCD_Test` в окремій папці
   - Наприклад: `C:\Users\ZbooK\OneDrive\Документи\Arduino\LCD_Test\LCD_Test.ino`

2. **Скопіюйте цей код у новий скетч:**

```cpp
/*
  Простий тест LCD 1602 (4-bit) для Arduino Micro
  Використовується для діагностики проблем з відображенням
  
  Підключення:
  LCD RS → D12
  LCD E  → D11
  LCD D4 → D7
  LCD D5 → D6
  LCD D6 → D5
  LCD D7 → D8
  LCD RW → GND
  LCD V0 → Потенціометр (середній вивід)
  LCD VSS → GND
  LCD VDD → 5V
*/

#include <LiquidCrystal.h>

// Піни для 4-bit LCD (згідно з Config.h)
const int rs = 12, en = 11, d4 = 7, d5 = 6, d6 = 5, d7 = 8;

LiquidCrystal lcd(rs, en, d4, d5, d6, d7);

void setup() {
  // Ініціалізація LCD 16x2
  lcd.begin(16, 2);
  delay(100); // Затримка для стабілізації
  
  // Виведення тестового тексту
  lcd.setCursor(0, 0);
  lcd.print("LCD Test OK!");
  lcd.setCursor(0, 1);
  lcd.print("Arduino Micro");
  
  delay(2000);
  
  // Очищення та виведення нового тексту
  lcd.clear();
  lcd.setCursor(0, 0);
  lcd.print("Pin Check:");
  lcd.setCursor(0, 1);
  lcd.print("RS=12 E=11");
  
  delay(2000);
  lcd.clear();
}

void loop() {
  // Бігуча стрічка
  static unsigned long lastUpdate = 0;
  static int pos = 0;
  static int line = 0;
  
  if (millis() - lastUpdate > 300) {
    lastUpdate = millis();
    
    // Очищення поточного положення
    lcd.setCursor(pos, line);
    lcd.print(" ");
    
    // Збільшення позиції
    pos++;
    
    // Перехід на новий рядок
    if (pos >= 16) {
      pos = 0;
      line++;
      if (line >= 2) {
        line = 0;
      }
    }
    
    // Виведення маркера
    lcd.setCursor(pos, line);
    lcd.print(">");
    
    // Виведення тексту на іншому рядку
    lcd.setCursor(0, 1 - line);
    lcd.print("Test running  ");
  }
  
  // Кожні 5 секунд показувати статичний текст
  static unsigned long lastStatic = 0;
  if (millis() - lastStatic > 5000) {
    lastStatic = millis();
    lcd.clear();
    lcd.setCursor(0, 0);
    lcd.print("Working!");
    lcd.setCursor(0, 1);
    lcd.print("Time: ");
    lcd.print(millis() / 1000);
    lcd.print("s");
    delay(1000);
    lcd.clear();
    pos = 0;
    line = 0;
  }
}
```

3. **Завантажте тестовий скетч на Arduino Micro**

**Якщо цей тестовий код працює:**
- ✅ LCD працює на апаратному рівні
- ✅ Проблема в основному коді (може бути конфлікт з енкодером, меню або ініціалізацією)
- Перевірте `Config.h` - чи правильно вибрано `LCD_INTERFACE_PARALLEL_4BIT`
- Перевірте `LCDDisplay.cpp` - чи правильно викликається `lcd.begin()`

**Якщо цей тестовий код НЕ працює:**
- ⚠️ Проблема в апаратному забезпеченні
- Перевірте підключення пінів (особливо D4-D7 на LCD)
- Перевірте контраст (V0) - покрутіть потенціометр
- Перевірте живлення (5V стабільно?)
- Перевірте GND (спільний GND між Arduino і LCD)

---

### 11. **ПЕРЕВІРКА ПОСЛІДОВНОСТІ ІНІЦІАЛІЗАЦІЄЮ**

**В основному коді перевірте `setup()`:**

```cpp
void setup() {
  analogReference(DEFAULT);
  loadSettings();
  encoder.begin();              // 1. Енкодер
  lcdDisplay.showStartup();     // 2. LCD (викликає begin() всередині)
  // ... menu manager
}
```

**Можлива проблема:**
- Енкодер може блокувати піни D2, D3
- Але це не має впливати на LCD (який використовує D5-D8, D11, D12)

---

### 12. **ЧАСТИ ПРОБЛЕМИ З ARDUINO MICRO**

**Особливості Arduino Micro:**
- Піни D2, D3 використовуються для USB Serial
- При завантаженні скетчу може бути конфлікт
- **Рішення:** Після завантаження скетчу відключіть і знову підключіть USB

**Перевірка:**
1. Завантажте скетч
2. Відключіть USB
3. Зачекайте 2 секунди
4. Підключіть USB знову
5. Спостерігайте за LCD

---

## 🎯 ШВИДКА ДІАГНОСТИКА (ПО ПОРЯДКУ):

1. ✅ **Перевірте Config.h** - `LCD_INTERFACE_PARALLEL_4BIT` має бути активний
2. ✅ **Перекомпілюйте та завантажте скетч**
3. ✅ **Перевірте контраст** - покрутіть потенціометр V0
4. ✅ **Перевірте підключення пінів** - особливо D4-D7 на LCD (не плутати з Arduino!)
5. ✅ **Запустіть тестовий код** `LCD_Test.ino`
6. ✅ **Перевірте живлення** - можливо недостатньо струму
7. ✅ **Перевірте GND** - має бути спільний GND між всіма компонентами

---

## 📞 ЯКЩО НІЧЕГО НЕ ДОПОМОГЛО:

1. **Спробуйте інший LCD модуль** (якщо є)
2. **Спробуйте іншу Arduino плату** (Uno або Nano)
3. **Перевірте всі з'єднання паяльником** (якщо використовуєте макетну плату)
4. **Перевірте мультиметром** всі піни на наявність сигналів
5. **Спробуйте мінімальний тест** - тільки LCD без енкодера і сенсора

---

## ✅ ОЧІКУВАНІ РЕЗУЛЬТАТИ:

Після виправлення конфігурації ви маєте побачити:

```
┌──────────────────┐
│ P3022 Sensor     │
│ Micro 32U4       │
└──────────────────┘
```

Потім через 0.8 секунди:

```
┌──────────────────┐
│ Ready            │
│ Press for menu   │
└──────────────────┘
```

А потім головний екран з кутом:

```
┌──────────────────┐
│ Angle:  45.23°   │
│                  │
└──────────────────┘
```

---

## 💡 ПОРАДА:

**Найчастіша проблема** - неправильна конфігурація в `Config.h`:
- Переконайтеся, що `#define LCD_INTERFACE_PARALLEL_4BIT` **НЕ закоментовано**
- Переконайтеся, що `#define LCD_INTERFACE_I2C` **закоментовано**

**Друга за частотою проблема** - неправильне підключення пінів D4-D7:
- LCD D4 → Arduino D7 (НЕ D4!)
- LCD D5 → Arduino D6 (НЕ D5!)
- LCD D6 → Arduino D5 (НЕ D6!)
- LCD D7 → Arduino D8 (НЕ D7!)

---

**Успіхів у діагностиці! 🚀**
